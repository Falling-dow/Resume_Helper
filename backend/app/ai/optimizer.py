def optimize_resume(resume_content: dict, job_description: str, user_preferences: dict | None = None) -> dict:
    # TODO: implement with LLMs via LangChain / OpenAI
    return {
        "optimized": True,
        "sections": [],
        "suggestions": {"add_keywords": ["微服务", "异步IO"]},
    }

